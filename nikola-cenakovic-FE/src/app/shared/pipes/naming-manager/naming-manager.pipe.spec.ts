import { NamingManagerPipe } from './naming-manager.pipe';

describe('NamingManagerPipe', () => {
  it('create an instance', () => {
    const pipe = new NamingManagerPipe();
    expect(pipe).toBeTruthy();
  });
});
